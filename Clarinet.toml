[project]
name = "gasless-pipe"
authors = ["Gasless Pipe Team"]
description = "A decentralized identity management infrastructure with gasless transaction capabilities"
telemetry = false
requirements = []
[contracts.identity-registry]
path = "contracts/identity-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
